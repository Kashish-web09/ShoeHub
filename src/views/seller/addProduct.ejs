<div class="container-fluid py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-1">Add New Product</h2>
            <p class="text-muted mb-0">
                Fill in the product details to add it to your store.
            </p>
        </div>

        <a href="/api/seller/product" class="btn btn-outline-dark">
            <i class="bi bi-arrow-left me-2"></i>
            Back to Products
        </a>
    </div>

    <div class="card shadow border-0">

        <div class="card-header bg-dark text-white">
            <h5 class="mb-0">
                <i class="bi bi-box-seam me-2"></i>
                Product Information
            </h5>
        </div>

        <div class="card-body">

            <form
                action="/api/seller/product/add-product"
                method="POST"
                enctype="multipart/form-data">

                <div class="row">

                    <!-- Product Name -->
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold">
                            Product Name
                        </label>

                        <input
                            type="text"
    class="form-control <%= errors?.name ? 'is-invalid' : '' %>"
                            name="name"
                            placeholder="Nike Air Max 270"
                            >
<% if (errors?.name) { %>
<div class="invalid-feedback">
    <%= errors.name.msg %>
</div>
<% } %>                    </div>

                    <!-- Brand -->
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold">
                            Brand
                        </label>

                        <input
                            type="text"
                            class="form-control <%= errors?.brand ? 'is-invalid': ' ' %>"
                            name="brand"
                            placeholder="Nike"
                            >
                            <% if(errors?.brand){ %>
<div class="invalid-feedback">
    <%= errors.brand.msg %>
</div>
<% } %>
                    </div>

                    <!-- Category -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Category
                        </label>

                        <select
                            class="form-select <%= errors?.category ? 'is-invalid': ' ' %>"
                            name="category"
                            >

                            <option value="">Select Category</option>
                            <option>Running</option>
                            <option>Sports</option>
                            <option>Casual</option>
                            <option>Formal</option>
                            <option>Sneakers</option>
                            <option>Boots</option>
                            <option>Sandals</option>

                        </select>
                        <% if(errors?.category){ %>
<div class="invalid-feedback">
    <%= errors.category.msg %>
</div>
<% } %>

                    </div>
   <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Gender
                        </label>

                        <select
                            class="form-select <%= errors?.gender ? 'is-invalid': ' ' %>"
                            name="gender"
                            >

                            <option value="">Select Gender</option>
                            <option>Mens</option>
                            <option>Womens</option>

                        </select>
<% if(errors?.gender){ %>
<div class="invalid-feedback">
    <%= errors.gender.msg %>
</div>
<% } %>
                    </div>
                    <!-- Price -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Price (₹)
                        </label>

                        <input
                            type="number"
                            class="form-control <%= errors?.price ? 'is-invalid': ' ' %>"
                            name="price"
                            min="1"
                            placeholder="2999"
                            >
<% if(errors?.price){ %>
<div class="invalid-feedback">
    <%= errors.price.msg %>
</div>
<% } %>
                    </div>

                    <!-- Stock -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Stock
                        </label>

                        <input
                            type="number"
                            class="form-control <%= errors?.stock ? 'is-invalid': ' ' %>"
                            name="stock"
                            min="0"
                            placeholder="50"
                            >
<% if(errors?.stock){ %>
<div class="invalid-feedback">
    <%= errors.stock.msg %>
</div>
<% } %>
                    </div>

                    <!-- Colors -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Colors
                        </label>

                        <input
                            type="text"
                            class="form-control <%= errors?.color ? 'is-invalid': ' ' %>"
                            name="color"
                            placeholder="Black, White, Blue">
<% if(errors?.color){ %>
<div class="invalid-feedback">
    <%= errors.color.msg %>
</div>
<% } %>
                        <small class="text-muted">
                            Separate multiple colors using commas.
                        </small>

                    </div>

                    <!-- Sizes -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Sizes
                        </label>

                        <input
                            type="text"
                            class="form-control <%= errors?.size ? 'is-invalid': ' ' %>"
                            name="size"
                            placeholder="6,7,8,9,10">
<% if(errors?.size){ %>
<div class="invalid-feedback">
    <%= errors.size.msg %>
</div>
<% } %>
                        <small class="text-muted">
                            Separate multiple sizes using commas.
                        </small>

                    </div>

                    <!-- Image -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Product Images
                        </label>

                        <input
                            type="file"
                            class="form-control <%= errors?.image ? 'is-invalid': ' ' %>"
                            name="images"
                            multiple
                            accept="image/*">
<% if(errors?.thumbnail){ %>
<div class="invalid-feedback d-block">
    <%= errors.thumbnail.msg %>
</div>
<% } %>
                    </div>

                    <!-- Thumbnail -->
                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-semibold">
                            Thumbnail Preview
                        </label>

                        <div>
                            <img
                                id="preview"
                                src="https://placehold.co/180x180?text=Preview"
                                class="img-thumbnail"
                                style="height:180px;width:180px;object-fit:cover;">
                        </div>

                    </div>

                    <!-- Description -->
                    <div class="col-md-12 mb-4">

                        <label class="form-label fw-semibold">
                            Product Description
                        </label>

                        <textarea
                            class="form-control <%= errors?.desc ? 'is-invalid': ' ' %>"
                            rows="5"
                            name="desc"
                            placeholder="Write complete product description..."
                            ></textarea>
                            <% if(errors?.desc){ %>
<div class="invalid-feedback">
    <%= errors.desc.msg %>
</div>
<% } %>

                    </div>

                    <!-- Best Seller -->
                    <div class="col-md-12 mb-4">

                        <div class="form-check form-switch">

                            <input
                                class="form-check-input "
                                type="checkbox"
                                name="isBestSeller"
                                value="true">

                            <label class="form-check-label">
                                Mark as Best Seller
                            </label>

                        </div>

                    </div>

                </div>

                <hr>

                <div class="d-flex justify-content-end gap-2">

                    <a
                        href="/api/seller/product"
                        class="btn btn-secondary">

                        Cancel

                    </a>

                    <button
                        type="reset"
                        class="btn btn-warning">

                        Reset

                    </button>

                    <button
                        type="submit"
                        class="btn btn-success">

                        <i class="bi bi-plus-circle me-2"></i>

                        Add Product

                    </button>

                </div>

            </form>

        </div>

    </div>

</div>

<script>

const preview = document.getElementById("preview");

document.querySelector("input[name='images']")
.addEventListener("change",function(){

    if(this.files.length>0){
        preview.src=URL.createObjectURL(this.files[0]);
    }

});

</script>